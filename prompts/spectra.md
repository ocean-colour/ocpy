# Generate methods for handling ocean color spectra

## Goals

We wish to generate Python code to handle ocean color spectra.  These include Rrs, a, bb, etc.  

## Requirements

At the simplest level, we want a very lightweight class that carries:

  - The wavelengths
  - The values
  - The errors
  - The units
  - The metadata
  - The source of the data
  - The date of the data
  - The location of the data
  - The depth of the data
  - The quality of the data

These items should be basic Python objects (numpy arrays, floats, etc.).

We then want methods that act on them.  Whereever possible, these methods will operate on basic Python objects (numpy arrays, floats, etc.) rather than classes.

## Claude

### Skills

Consider using the skills in .claude/skills/

## Context

Examine the file ocpy/ocpy/utils/spectra.py (and the modules that use it) to see how we handle spectra currently.  We are going to refactor this code to be more general and robust.

Examine the new ocpy/ocpy/insitu/panagea.py module to see how we handle the PANAGEA dataset.  It contains examples of Hyperspectral Rrs spectra.

Examine the ocpy/ocpy/hydrolight/loisel23.py module to see how we handle the Loisel+2023 Hydrolight datasets.  These are hyperspectral

## Data

The Loisel+2023 Hydrolight datasets are located in $OS_COLOR/Loisel2023
The PANAGEA dataset is located in $OS_COLOR/PANAGEA/V3

## Code

### Writing

Here are guidelines for writing code:

- Use Python
- Add inline comments to explain the effort
- Reuse existing code when possible
- When possible use methods, not classes
- Use lines of code that are less than 80 characters wide
- Include doc strings for all functions and methods, including parameters and return values
- Add "Generated by JXP and Claude" in the doc string of all methods you create

### Running

If you need to run Python, use the "ocean14" Conda environment.

## Prompts

1. Read this doc.  Execute the 1st task under "Planning".
2. Read this doc.  Execute the 2nd task under "Planning".
3. Read this doc.  Execute the 3rd task under "Planning".
4. Read this doc.  Execute the 4th task under "Planning".
5. Read this doc.  Execute the 5th task under "Planning".

6. Read this doc.  Execute the 1st task under "Implement".
7. Read this doc.  Execute the 2nd task under "Implement".
8. Read this doc.  Execute the 3rd task under "Implement".
9. Read this doc.  Execute the 4th task under "Implement".
10. Read this doc.  Execute the 5th task under "Implement".

## Implement

### Tasks

1. Read the Plan and proceed with Phase 0.  If any issues arise, ask them in the Q&A section below.  Log your work
2. Read the Plan and proceed with Phase 1.  If any issues arise, ask them in the Q&A section below.  Log your work
3. Read the Plan and proceed with Phase 2.  If any issues arise, ask them in the Q&A section below.  Log your work
4. Read the Plan and proceed with Phase 3.  Regarding your concern from Phase 2, it is ok if the user cannot access ds['lat'] for now. If any issues arise, ask them in the Q&A section below.  Log your work

5. I have made the Tara data available to you.  Please complete Phase 3.

### Q&A

**Phase 0 — no blocking issues.** Two scope notes (decisions I made; flag if
you disagree):

- I created only `ocpy/spectra/utils.py` and left `ocpy/spectra/__init__.py`
  empty for now. The Plan lists `__init__.py` re-exporting `Spectrum` /
  `SpectrumStack`, but those classes don't exist until Phase 2 — populating
  the re-exports now would make `import ocpy.spectra` fail. I'll wire up
  `__init__.py` in Phase 2.
- Likewise I did **not** create empty `core.py` / `io.py` stubs; they'll be
  created with real content in Phases 2–3. Empty modules add nothing and risk
  confusing imports.

- Confirmed the four `from oceancolor.tara import spectra` hits are importers
  of `ocpy/tara/spectra.py` (a different module), **not** `ocpy.utils.spectra`
  — so they belong to Phase 4, and Phase 0 needed no caller edits.

**Phase 1 — no blocking issues.** Notes:

- **`rebin_to_grid` API change (heads-up for Phase 4):** the input is now
  spectrum-major `(nspec, nwave)` (was wavelength-major `(nwave, nspec)`).
  Tara's `spectbl_from_keys` returns `(nwave, nspec)`, so the call in
  `tara/explore.py` will need a transpose (or to pass `.T`) when migrated.
- **One extra robustness fix beyond the shape change:** empty output bins now
  fill **both** values and errors with NaN (the old code left values at 0.0,
  a latent bug), and the all-NaN-bin division is wrapped in
  `np.errstate(...)` to suppress warnings. Flag if you'd rather keep the old
  zero-fill for values.

**Phase 2 — no blocking issues.** One design deviation worth your sign-off:

- **Stack provenance is stored as a single JSON attr, not as per-spectrum 1D
  variables.** The Plan said `SpectrumStack.to_xarray()` would write `date`,
  `lat`, `lon`, `depth`, `source` as 1D variables over `spectrum`. In
  practice, netCDF string arrays (needed for `date`/`source`) are fragile
  across engines, and per-spectrum `quality`/`metadata` dicts can't be 1D
  vars at all. I instead store the full per-spectrum provenance as a JSON
  list-of-dicts in `ds.attrs['provenance_json']` (reusing the single-spectrum
  attrs builder). This is netCDF-safe and round-trips with full fidelity
  (verified by tests). Trade-off: you can't `ds['lat']` to slice provenance
  inside xarray — use the `SpectrumStack.lats` accessor instead. Say the word
  if you want numeric `lat`/`lon`/`depth` promoted to real 1D variables (easy
  to add alongside the JSON).
- Wired up `ocpy/spectra/__init__.py` (the item deferred from Phase 0):
  it now re-exports `Spectrum`, `SpectrumStack`, and the five helper
  functions.
- xarray/matplotlib imports are lazy (inside the methods) to keep the classes
  lightweight to import.

**Phase 3 — no blocking issues.** Notes / minor deviations:

- **`from_tara` takes a row, not `(tbl, row)`.** The Plan listed
  `from_tara(tbl, row, flavor='ap')`, but `tara.spectra.spectrum_from_row`
  only needs the row, so the adapter is `from_tara(row, flavor='ap')`
  (cleaner; `stack_from_tara(tbl, flavor=)` iterates rows). Flag if you want
  the `tbl` argument back.
- **Loisel23 units (Q18):** confirmed the `.nc` files record **no**
  per-variable units (empty attrs); `Lambda` is a shared coordinate (81
  bands, 350 nm+). So `from_loisel23(..., units=None)` by default, with a
  `units=` kwarg to pass the known convention (Rrs=1/sr, a*/b*=1/m). I did
  not hard-code units to avoid asserting unverified metadata.
- **Tara provenance** column names are unknown (no local Tara data), so
  `from_tara` looks up `lat`/`lon`/`date` defensively across candidate names
  and leaves them None if absent. Verified end-to-end on a synthetic row.
- PANAGEA adapter pulls units from the column metadata (`'1/sr'` for rrs),
  date/lat/lon/depth from the row, QF flags into `quality`, and
  dataset/subdataset/contributor into `metadata`.

## Planning

### Tasks

1. Read the Requirements and examine the files in the Context section.  Put your findings in the Findings section below.  If you have any questions, write them in the Q&A section below. Log your work.

2. I have read your findings and I have answered most of your questions.  Read those, modify your findings and ask more questions if needed.

3. Ok, I like your recommendations: 

    - keep the lightweight class(es) for spectra and provide `to_xarray()` and `from_xarray()` adapters.
    - have two objects, Spectrum and SpectrumStack.

Please generate a Plan for the refactor.  Write the plan in the Plan section below.  The plan should include tests, notebooks, docs, etc.  If you have any questions, write them in the Q&A section below. Log your work.

4. I had missed your extra round of questions.   I have now answered them.  Read those, modify your plan and ask more questions if needed.

5. I have answered your additional 2 questions.  Read those and modify your plan.

### The Plan

This plan refactors spectrum handling into the `ocpy/spectra/` package with
two lightweight, numpy-backed classes (`Spectrum`, `SpectrumStack`),
array-level helper functions, source adapters, and `xarray` interop. It is
organized in phases so each can be reviewed/committed independently.

**Decisions folded in from Q&A rounds 2–3:**
- Q11 (confirmed): a stack stores a list of `Spectrum`; the 2D gridded array
  is materialized lazily on `as_array()` / `to_xarray()`.
- Q12 (confirmed): each stack member keeps its own provenance; the stack
  exposes per-spectrum arrays (`dates`, `lats`, `lons`, `depths`).
- Q15: in the xarray representation, **wavelength is a data Variable, not a
  coordinate** (see Phase 2). This also lets ragged stacks serialize by
  NaN-padding, so `to_xarray()` no longer needs to raise on ragged input.
- Q16: `to_netcdf()` / `read_netcdf()` wrappers are **in scope** on both
  classes.
- Q17: a matplotlib `plot()` method is **in scope**.
- Q18: Loisel23 variable keys/units confirmed against the data files during
  implementation.
- Q19 (confirmed): the bare index dimension is named `channel`.
- Q20 (confirmed): `date` is serialized to netCDF as an ISO-8601 string in
  `.attrs` and parsed back on read.

**Status: all questions answered — the plan is ready to implement.**

#### Phase 0 — Module skeleton & relocation

Target layout under `ocpy/spectra/`:

- `__init__.py` — public API re-exports: `Spectrum`, `SpectrumStack`, and the
  most-used helper functions.
- `utils.py` — array-level free functions (the "methods on basic objects").
  Move the two functions from `ocpy/utils/spectra.py` here verbatim first
  (so history/behaviour is preserved), then clean up in Phase 1.
- `core.py` — the `Spectrum` and `SpectrumStack` classes.
- `io.py` — source adapters (PANAGEA / Loisel23 / Tara) and the
  xarray conversion helpers used by the class methods.

Relocation steps:
1. Create `ocpy/spectra/utils.py` with the existing `rebin` and
   `rebin_to_grid` content; drop the `from IPython import embed` line.
2. Delete `ocpy/utils/spectra.py`.
3. `grep -rn "ocpy.utils.spectra\|utils import spectra\|oceancolor.*spectra"`
   and repoint every importer (see Phase 4).

#### Phase 1 — Array-level helpers (`ocpy/spectra/utils.py`)

Keep these as pure functions on numpy arrays (no class dependency), per the
Requirements. Each gets a docstring with "Generated by JXP and Claude" and
<80-char lines.

- `rebin(wv_nm, values, wv_grid, err_vals=None)` — kept (interp1d). Document
  that errors are interpolated, not formally propagated (matches Q7: "rebin
  handles them correctly").
- `rebin_to_grid(...)` — **fix the shape inconsistency** found in task 1:
  settle on `values` shape `(nspec, nwave)` consistently in the signature,
  body, and docstring; add a regression test pinning the corrected
  behaviour. (This is the one behavioural change; call it out in the log.)
- New small helpers extracted so both classes and callers can reuse them:
  - `value_at(wv_nm, values, wv, kind='linear')` — interpolate a single
    wavelength.
  - `common_grid(list_of_wv)` — test whether a set of wavelength arrays are
    identical (drives `SpectrumStack.is_gridded`).
  - `align_to_grid(list_of_wv, list_of_values, wv_grid, list_of_err=None)` —
    rebin a ragged collection onto one grid, returning stacked 2D arrays.

#### Phase 2 — Core classes (`ocpy/spectra/core.py`)

`Spectrum` — `@dataclass`, exactly one spectrum. Fields:

- `wavelength: np.ndarray`, `values: np.ndarray`, `errors: np.ndarray|None`
- `units: str|None`, `source: str|None`, `date: np.datetime64|None`
- `lat: float|None`, `lon: float|None`, `depth: float|None`
- `quality: dict|None` — named QF flags kept separate (Q6), e.g.
  `{'qf_time': ..., 'qf_chl': ...}`
- `metadata: dict` — free-form (richer location, sensor/band, etc.)

Behaviour & methods (thin wrappers delegating to `utils.py`):
- `__post_init__` validation: `wavelength`/`values` same length; `errors`
  same length if given; wavelength sorted (sort if not).
- `__len__`, `__repr__` (compact summary: n points, range, units, source).
- `rebin(wv_grid, method='interp')` → new `Spectrum`.
- `value_at(wv)` → float.
- `to_xarray()` → `xarray.Dataset`. **xarray layout (per Q15): wavelength is
  a data Variable, not a coordinate.** The dataset uses a bare index
  dimension `channel` (dimension-without-coordinate) and three data
  variables over it — `wavelength(channel)`, `values(channel)`,
  `errors(channel)`. Scalars/units/provenance go into `.attrs`. Naming the
  dim `channel` (not `wavelength`) prevents xarray from auto-promoting a
  same-named variable into a dimension coordinate.
- `from_xarray(ds)` (classmethod) → `Spectrum` (reads the `wavelength`
  variable back).
- `to_netcdf(path, **kw)` / `read_netcdf(path)` (classmethod) — thin wrappers
  over `to_xarray().to_netcdf(...)` and `from_xarray(xarray.open_dataset)`
  (Q16). Provenance is round-tripped via `.attrs`; `date` stored as an ISO
  string for netCDF-safety.
- `plot(ax=None, **kw)` → matplotlib line, errors as a shaded band when
  present; creates an axis if none given and returns it (Q17).

`SpectrumStack` — `@dataclass`, collection backed by `list[Spectrum]`:
- `spectra: list[Spectrum]`
- `is_gridded` (property) — all members share one wavelength grid
  (via `utils.common_grid`).
- Per-spectrum provenance accessors: `dates`, `lats`, `lons`, `depths`,
  `sources` → numpy arrays (Q12).
- `as_array()` — when gridded, return `(wavelength, values_2d, errors_2d)`;
  raise (with a clear message) if ragged, pointing at `rebin`.
- `rebin(wv_grid, method='interp')` → a new, gridded `SpectrumStack`.
- `to_xarray()` — wavelength as a data Variable lets us serialize **both**
  gridded and ragged stacks (resolves Q15 — no need to raise). Layout: dims
  `(spectrum, channel)`, both bare index dims; data variables
  `wavelength(spectrum, channel)`, `values(spectrum, channel)`,
  `errors(spectrum, channel)`; provenance as 1D variables over `spectrum`
  (`date`, `lat`, `lon`, `depth`, `source`). Ragged members NaN-pad to the
  max channel count (the wavelength variable records the real grid per row,
  so padding is unambiguous).
- `from_xarray(ds)` (classmethod) — reconstruct members, dropping NaN pad.
- `to_netcdf(path, **kw)` / `read_netcdf(path)` (classmethod) — wrappers as
  for `Spectrum` (Q16).
- `__len__`, `__iter__`, `__getitem__`.

#### Phase 3 — Source adapters (`ocpy/spectra/io.py`)

Reuse the existing loaders; these only *map* their output into the classes:
- `from_panagea(df, obs_id, kind='rrs')` → `Spectrum`, pulling wavelength +
  value from `panagea.spectrum()`, units/metadata from
  `panagea.column_metadata()`, and `date`/`lat`/`lon`/`depth`/QF from the
  row. A `stack_from_panagea(df, kind=, ids=None)` → `SpectrumStack`.
- `from_loisel23(ds, index, var='Rrs')` → `Spectrum` (shared `Lambda` coord);
  `stack_from_loisel23(ds, var=)` → gridded `SpectrumStack`.
- `from_tara(tbl, row, flavor='ap')` → `Spectrum`, reusing
  `tara.spectra.parse_wavelengths` / `spectbl_from_keys`;
  `stack_from_tara(tbl, flavor=)` → `SpectrumStack`.

#### Phase 4 — Migrate callers & fix stale imports (Q1)

- `ocpy/tara/measures.py`, `ocpy/tara/explore.py`,
  `ocpy/tests/test_tara.py`, `runs/Tara/Figures/py/figs_explore_tara.py`:
  change `from oceancolor.tara import spectra` →
  `from ocpy.tara import spectra`, and repoint any
  `oceancolor`/`utils.spectra` rebin call to `ocpy.spectra.utils`.
- Drop the dead `from IPython import embed` lines in `tara/spectra.py` and
  `hydrolight/loisel23.py` while touching those files.
- Re-run the grep from Phase 0 to confirm zero remaining `oceancolor`
  references in scope.

#### Phase 5 — Tests (`ocpy/tests/test_spectra.py`)

Follow the `test_panagea.py` pattern (data-independent tests always run;
data-dependent tests guarded by `pytest.mark.skipif` on `$OS_COLOR`).
- Data-independent (synthetic arrays):
  - `Spectrum` construction, validation, `__len__`/`__repr__`, sorting.
  - `value_at`, `rebin` round trips; **regression test for the corrected
    `rebin_to_grid` shape**.
  - `to_xarray`/`from_xarray` round-trip equality (values, errors, attrs);
    assert `wavelength` is a data variable, **not** a coordinate (Q15).
  - `to_netcdf`/`read_netcdf` round trip via a `tmp_path` file (Q16).
  - `plot()` smoke test (returns an `Axes`, no exception) (Q17).
  - `SpectrumStack`: gridded vs. ragged detection, `as_array()` raising on
    ragged, `rebin` → gridded, provenance arrays, and **ragged-stack
    `to_xarray`/`from_xarray` round trip** (NaN-pad then recover members).
- Data-dependent (skip-guarded): `from_panagea`, `from_loisel23`,
  `from_tara` produce correct shapes/units against the real files.
- Keep the existing `test_tara.py` green after the import migration.

#### Phase 6 — Notebook (`nb/Spectra/Spectra_demo.ipynb`)

A demo notebook mirroring the `nb/PANAGEA/` style: load a PANAGEA Rrs
spectrum and a Loisel23 spectrum into `Spectrum`, rebin both to a common
grid, build a `SpectrumStack`, plot, and round-trip through `to_xarray()`.

#### Phase 7 — Docs (`docs/`)

- `docs/spectra.rst` — narrative user guide (class overview, the
  basic-objects philosophy, adapters, xarray interop), linked from
  `index.rst`.
- API autodoc stub under `docs/api/` for `ocpy.spectra` (matching the
  existing `docs/api` layout).
- One-line `docs/changelog.rst` entry.

#### Phase 8 — Verify

Run `pytest ocpy/tests/test_spectra.py ocpy/tests/test_tara.py` in the
`ocean14` env; execute the demo notebook top-to-bottom. Report results in the
Logs.

#### Out of scope (proposed)

- A controlled units vocabulary / astropy units (Q8: opaque string).
- Reorganizing `tara/spectra.py` internals beyond the import fix.
- Backwards-compat shim for `ocpy.utils.spectra` (Q2: moved wholesale).

### Findings

#### How spectra are handled today

There is no single, general spectrum abstraction in `ocpy`. Spectra are
handled ad hoc, per data source, almost always as bare numpy arrays plus
loosely-associated wavelength arrays:

- **`ocpy/utils/spectra.py`** — two free functions only, no class:
  - `rebin(wv_nm, values, wv_grid, err_vals=None)` — interpolates a single
    spectrum (and optional errors) onto a new grid via `scipy.interp1d`,
    `fill_value=np.nan` outside bounds. Returns `(new_values, new_err)`.
  - `rebin_to_grid(wv_nm, values, err_vals, wv_grid)` — nearest-neighbour
    bin averaging (no interpolation) for a *stack* of spectra. Note the
    shape convention is awkward/inconsistent: the docstring says values are
    `(nwave, nspec)` but the loop indexes `values.shape[1]` as the spectrum
    count and returns `(nspec, nwv)`. Worth fixing in the refactor.
  - Both operate purely on numpy arrays — already aligned with the
    "methods over classes, basic objects" requirement.

- **`ocpy/tara/spectra.py`** — Tara-specific extraction: `parse_wavelengths`
  pulls wavelengths out of column-name strings (e.g. `ap440`), and
  `spectbl_from_keys` returns `(values, err)` arrays of shape
  `(len(keys), len(tbl))`. Wavelength/value/error are kept as separate,
  caller-managed arrays. `tara/explore.py` then calls
  `utils.spectra.rebin_to_grid` on those arrays.

- **`ocpy/insitu/panagea.py`** — the most mature representation. A loaded
  dataset is a `pandas.DataFrame` indexed by observation `ID`, with rich
  per-column metadata stashed in `df.attrs['columns']` (orig name, unit,
  parsed wavelength, sensor/band, role, long_name). `spectrum(df, obs_id,
  kind='rrs')` returns a `pandas.Series` of values indexed by wavelength
  (sorted, NaNs dropped). Handles both native-wavelength and satellite-band
  layouts. Carries unit and metadata, but errors are *not* paired with
  values, and per-observation provenance (date, lat/lon, depth, QF) lives in
  separate DataFrame columns rather than travelling with an extracted
  spectrum.

- **`ocpy/hydrolight/loisel23.py`** — Loisel+2023 Hydrolight outputs are
  `xarray.Dataset`s with a `Lambda` coordinate and variables `aph`, `bb`,
  etc., shape `(nspec, nwave)`. `calc_Chl` indexes `aph` at 440 nm. No unit
  metadata exposed; wavelength is shared across all spectra in a file.

#### Cross-cutting observations

1. **Three distinct on-disk layouts** the new abstraction must bridge:
   bare numpy arrays (utils/tara), wavelength-indexed pandas Series/wide
   DataFrame (PANAGEA), and xarray Dataset with a shared `Lambda` coord
   (Loisel23). A lightweight class should be constructible from all three.
2. **Errors are inconsistently carried** — present in utils/tara, absent in
   PANAGEA `spectrum()` and Loisel23.
3. **Provenance fields the requirements ask for** (source, date, location,
   depth, quality) already exist in PANAGEA as DataFrame columns but are not
   attached to an individual extracted spectrum. The new class would gather
   them into one object.
4. **An empty `ocpy/spectra/` package already exists**
   (`ocpy/spectra/__init__.py`, 0 bytes) — the natural home for the new
   general `Spectrum` class and the refactored array-level methods.
5. **Stale imports**: `tara/measures.py`, `tara/explore.py`,
   `tests/test_tara.py`, and a `runs/` script still
   `from oceancolor.tara import spectra` (pre-rename). See
   [[ocpy-package-rename]]. The refactor is a chance to migrate these to the
   new home, but that is a larger blast radius than this prompt scopes.
6. **`from IPython import embed`** is left in `utils/spectra.py`,
   `tara/spectra.py`, and `loisel23.py` — dead debug imports to drop.
7. **Units are free-form strings** from PANGAEA (`'1/sr'`, `'1/m'`,
   `'mg/l'`). The class should store units as-is (a string), not try to
   enforce a controlled vocabulary, per the "basic Python objects"
   requirement.

#### Decisions (settled in Q&A round 1)

- **Callers WILL be migrated** and the stale `oceancolor` imports fixed
  (Q1). Blast radius: `tara/measures.py`, `tara/explore.py`,
  `tests/test_tara.py`, and the `runs/Tara` script.
- **Code home** (Q2): the new class lives in the `ocpy/spectra/` package;
  the existing `ocpy/utils/spectra.py` functions move wholesale to
  `ocpy/spectra/utils.py` (the array-level methods). Implies updating any
  importer of `ocpy.utils.spectra` too.
- **`location`** (Q5): `lat` and `lon` kept as separate floats; any richer
  location info (station, cruise) goes in the `metadata` dict.
- **`quality`** (Q6): keep QF flags separate when the source provides them
  separately (i.e. don't fold them into a single scalar).
- **`errors`** (Q7): 1-sigma uncertainties; current rebin/interp handling is
  considered acceptable.
- **`units`** (Q8): opaque string, no unit-awareness library.

#### Open discussion A — representation: xarray vs. lightweight class (Q3)

The user is weighing using an `xarray.Dataset` as *the* spectrum
representation (as Loisel23 already does). Pros and cons:

**Pros of an xarray Dataset**
- Native to Loisel23 → zero-friction adaptation for that source.
- Self-describing: a named `wavelength` coordinate indexes the values, much
  like PANAGEA's wavelength-indexed Series.
- `.attrs` (dataset- and variable-level) maps cleanly onto the requested
  provenance fields (units, source, date, lat, lon, depth, quality).
- Values + errors sit as two data variables sharing the wavelength coord.
- Free, well-tested machinery: `.interp`, `.sel`, automatic coordinate
  alignment/broadcasting, NaN handling, and netCDF serialization.
- The gridded-stack case (Q4) is natural: a 2D `(spectrum, wavelength)` data
  variable over a shared wavelength coord.

**Cons of an xarray Dataset**
- It is **not** a "basic Python object" — this is in direct tension with the
  Requirements ("very lightweight class... numpy arrays, floats, etc." and
  "methods... operate on basic Python objects... rather than classes").
- The **ragged-stack case** (different/different-length wavelength axes, the
  hard half of Q4) does not fit one Dataset cleanly: xarray aligns on the
  coordinate by NaN-filling the union grid, which wastes memory and conflates
  "not measured here" with "missing".
- `.attrs` must be netCDF-serializable to keep the I/O benefit, restricting
  what can be stashed (no arbitrary Python objects); attrs are untyped.
- Heavyweight and verbose for what is, for a single spectrum, a few arrays
  plus a handful of scalars.

**Recommendation (middle path):** keep the lightweight class (numpy arrays +
scalars) as the canonical in-memory object — honoring the "basic objects"
requirement and keeping the array-level methods in `ocpy/spectra/utils.py`
operating on plain numpy — and add thin `to_xarray()` / `from_xarray()`
adapters. That gives Loisel23 interop and netCDF I/O for free without paying
the xarray tax everywhere, and the ragged case stays expressible. See Q&A
round 2, Q9.

#### Open discussion B — single vs. stacked spectra (Q4)

The user notes: same wavelengths → can stack; different (and possibly
different-length) wavelengths → must hold separately. Likely two objects.

Proposed shape:
- **`Spectrum`** — exactly one spectrum: 1D `wavelength`, 1D `values`, 1D
  `errors`, plus the scalar provenance fields.
- **`SpectrumStack`** — a collection, backed by a **list of `Spectrum`** so
  ragged members (different grids/sizes) are always representable. It offers
  a fast-path **gridded** view — a 2D `(nspec, nwave)` array over one shared
  wavelength axis with per-spectrum provenance arrays — available when all
  members already share a grid, or after `rebin`/`align_to_grid` resamples
  them onto a common grid. So: ragged list is the general store; the 2D
  array is an optimization unlocked by a shared grid.

This cleanly separates the easy gridded case (Loisel23, rebinned Tara) from
the ragged case (raw PANAGEA/Tara with heterogeneous bands). See Q&A round 2,
Q10–Q12.

#### Other implications (unchanged)

- Operations (rebin, interpolate, slice-by-wavelength, value-at-wavelength,
  arithmetic) stay as free functions on numpy arrays in
  `ocpy/spectra/utils.py`, with thin class methods that delegate — extending
  the existing style.
- The class should expose adapters from a PANAGEA row, a Loisel23 Dataset
  row, and a Tara table row so the three sources converge.


### Q&A

1. **Scope of refactor vs. callers** — Should this prompt also migrate the
   existing callers (`tara/measures.py`, `tara/explore.py`,
   `tests/test_tara.py`) and fix the stale `oceancolor` imports, or only
   build the new general spectra module and leave existing call sites
   untouched for now?

Yes, migrate the callers and fix the stale `oceancolor` imports.

2. **Home for the code** — Put the new `Spectrum` class and refactored
   methods in the existing empty `ocpy/spectra/` package? And do we keep
   `ocpy/utils/spectra.py` as a thin re-export for back-compat, or move its
   functions wholesale?

Yes, put the class in the `ocpy/spectra/` package and move the existing `ocpy/utils/spectra.py` functions to the new `ocpy/spectra/` package in a module named `utils.py`.

3. **Class implementation style** — Plain class, `@dataclass`, or
   `attrs`/`pydantic`? The requirements say "basic Python objects" and
   "methods not classes" where possible; a `@dataclass` of numpy arrays
   seems to fit. Confirm `@dataclass` is acceptable.

I am now thinking it might be ideal to use an xarray Dataset to represent a spectrum, as per the Loisel2023 Hydrolight datasets.  Please discuss the pros and cons of this approach.

4. **Single vs. stacked spectra** — Should `Spectrum` model exactly one
   spectrum (one wavelength axis, one values vector), with stacks handled by
   a list / a separate `SpectrumStack`? Or should one object hold an
   `(nspec, nwave)` array (as Loisel23/Tara naturally provide)?

This is a tough one.  When the wavelengths are the same, we can stack them.  But when the wavelengths are different, we would need to hold these separately.  And they can have different sizes.  Let's continue to discuss this.  We may wind up with one object for a single spectrum, and another object for a stack 

5. **`location` representation** — lat/lon as a `(lat, lon)` tuple of
   floats? Anything else needed (e.g. station name, cruise)?

Let's keep lat and lon separate, as floats.  If there is additional metadata describing location, we should include it in a metadata dictionary.

6. **`quality` representation** — a single scalar/flag per spectrum, or a
   per-wavelength array of QF values (PANGAEA carries `QF time`, `QF Chl`)?

Keep QF flags separate when provided separately.

7. **Errors semantics** — are the "errors" 1-sigma uncertainties, and
   should rebinning propagate them (currently just interpolated/averaged)?

Yes, errors are 1-sigma uncertainties.  I believe the rebinning handles them correctly.

8. **Units enforcement** — store units as an opaque string, or do we want
   any unit-awareness (e.g. astropy units)? My assumption: opaque string.

Let's keep units as an opaque string.

#### Q&A round 2 (follow-ups)

9. **xarray as core vs. adapter (re: Q3)** — Do you agree with the
   middle-path recommendation above: lightweight numpy-backed `Spectrum` as
   the canonical object, with `to_xarray()` / `from_xarray()` adapters? Or do
   you want the `xarray.Dataset` to *be* the `Spectrum` (accepting the
   tension with the "basic Python objects" requirement and the ragged-stack
   limitation)?

10. **Stack backing (re: Q4)** — OK with `SpectrumStack` backed by a list of
    `Spectrum`, exposing a 2D gridded array only when members share a grid?
    Or would you rather have two explicit types (e.g. `GriddedStack` with a
    2D array vs. `RaggedStack`/list)?

11. **Auto-grid on construction** — When building a stack from a source that
    is already gridded (Loisel23, or rebinned Tara), should it default to the
    2D representation immediately, or always start as a list and only
    materialize the 2D array on request?

12. **Per-spectrum provenance in a stack** — Confirm each member of a stack
    keeps its own `date`/`lat`/`lon`/`depth`/`quality` (i.e. the stack stores
    these as per-spectrum arrays), since PANAGEA/Tara stacks mix observations
    from different times and places.

13. **netCDF / serialization scope** — Is round-trip I/O (e.g. save/load a
    `Spectrum` or `SpectrumStack` to netCDF) in scope for this work, or
    deferred? This affects how strict we are about attrs being serializable.

#### Q&A round 3 (to confirm the plan)

Q9/Q10 are treated as answered by task 3 (keep lightweight classes +
xarray adapters; two objects). Q11–Q13 still open; the plan assumes the
defaults below — please confirm or correct:

14. **Lazy gridding (re: Q11)** — Plan default: a `SpectrumStack` always
    stores a list and only materializes the 2D array on `as_array()` /
    `to_xarray()`. OK?

Yes, this is ok.

15. **Ragged `to_xarray()` behaviour** — When a stack is ragged, should
    `to_xarray()` raise (forcing an explicit `rebin` first) or silently
    auto-rebin to the union grid? Plan default: **raise**.

Can we have the x-array provide the wavelengths as a separate Variable and not as a coordinate?

16. **netCDF I/O (re: Q13)** — Add `Spectrum.to_netcdf()/read_netcdf()`
    wrappers now, or defer? Plan default: defer (rely on `to_xarray()` +
    user calling `.to_netcdf()` on the Dataset).

yes, let's add these

17. **`plot()` method** — Is a convenience `Spectrum.plot()` wanted, or keep
    plotting out of the lightweight class? Plan default: include a minimal
    one, low priority.

Yes, add a plot method.  Use matplotlib 

18. **Loisel23 variable names** — `from_loisel23` needs the canonical
    variable keys (e.g. is reflectance `Rrs`, and what are the `bb`/`a`
    keys?) and whether a per-variable unit string is recorded anywhere. I'll
    confirm against the data files in the implement phase, but flag it here.

You can confirm against the data files in the implement phase.

#### Q&A round 4 (after folding in round-3 answers)

The plan is updated and I have no blocking questions. Two minor confirmations
(the plan picks a sensible default for each; correct me if wrong):

19. **xarray dim name** — I named the bare index dimension `channel` (with
    `wavelength` as a data variable over it) so xarray won't auto-promote a
    same-named variable into a coordinate. Happy with `channel`, or prefer
    another name (e.g. `wave_index`)?

Yes, channel is fine.

20. **`date` serialization in netCDF** — I plan to store `date` as an ISO-8601
    string in `.attrs` for netCDF safety and parse it back on read. OK, or do
    you want `np.datetime64` preserved via xarray's native time encoding?

Yes, let's store the date as an ISO-8601 string in the .attrs.

## Logging

The "Logs" section will record Claude's work.  Please use the following format:

### <Date> (Short summary of the work)

<Detailed description of the work and what you learned>

### <Date> (Short summary of the work)

<Detailed description of the work and what you learned>

...

## Logs

### 2026-06-16 (Planning task 1: surveyed current spectra handling)

Read the prompt and examined the Context files plus the broader `ocpy`
codebase for how spectra are handled today.

Files reviewed:
- `ocpy/utils/spectra.py` — `rebin` (interp1d) and `rebin_to_grid`
  (nearest-neighbour bin averaging); pure numpy, no class. Noted a shape /
  docstring inconsistency in `rebin_to_grid`.
- `ocpy/insitu/panagea.py` — DataFrame-based, rich per-column metadata in
  `df.attrs['columns']`; `spectrum()` returns a wavelength-indexed Series;
  handles native and satellite-band layouts; errors not paired with values.
- `ocpy/hydrolight/loisel23.py` — xarray Datasets with a shared `Lambda`
  coord; `(nspec, nwave)` variables; no unit metadata.
- `ocpy/tara/spectra.py` + callers — wavelengths parsed from column names;
  values/errors returned as separate arrays; feeds `rebin_to_grid`.

Key learnings: spectra are handled ad hoc per source across three distinct
on-disk layouts (numpy arrays, wide/long pandas, xarray). Errors and
provenance (source/date/location/depth/quality) are carried inconsistently
and rarely travel with an extracted spectrum. An empty `ocpy/spectra/`
package already exists as a natural home. Several modules still import from
the pre-rename `oceancolor` namespace, and `from IPython import embed` debug
lines linger. Recorded these in **Findings** and raised eight clarifying
questions in **Q&A** (scope, code home, class style, single vs. stacked,
location/quality/error semantics, units). Did not write code or draft The
Plan — those belong to later tasks.

### 2026-06-16 (Planning task 2: folded in answers, opened design discussion)

Read the user's Q&A answers and updated **Findings** accordingly. Settled
decisions: migrate callers + fix `oceancolor` imports (Q1); new class in
`ocpy/spectra/`, move `utils/spectra.py` → `ocpy/spectra/utils.py` (Q2);
`lat`/`lon` as separate floats with extra location info in `metadata` (Q5);
keep QF flags separate (Q6); errors are 1-sigma (Q7); units opaque string
(Q8). Recorded these in a new "Decisions" subsection.

Two questions are still open and were turned into discussion sections:
- **A (Q3, xarray):** wrote out pros/cons of using an `xarray.Dataset` as the
  spectrum representation. xarray is native to Loisel23 and self-describing
  with strong machinery, but clashes with the "basic Python objects"
  requirement and handles ragged stacks poorly. Recommended a middle path:
  numpy-backed lightweight class as canonical, with `to_xarray()` /
  `from_xarray()` adapters.
- **B (Q4, stacking):** proposed `Spectrum` (single) + `SpectrumStack`
  (list-of-`Spectrum`, with a 2D gridded fast path when members share a
  grid; ragged otherwise).

Raised five round-2 follow-up questions (Q9–Q13) covering the xarray
decision, stack backing/auto-gridding, per-spectrum provenance in stacks, and
whether netCDF serialization is in scope. Still no code written — The Plan
remains for a later task once Q9–Q13 are answered.

### 2026-06-16 (Planning task 3: drafted the refactor plan)

User approved the two key recommendations (lightweight classes +
`to_xarray()`/`from_xarray()` adapters; two objects `Spectrum` and
`SpectrumStack`). Wrote a phased plan into **The Plan**:

- Phase 0: create `ocpy/spectra/{utils,core,io}.py`; relocate
  `ocpy/utils/spectra.py` → `ocpy/spectra/utils.py` and delete the original.
- Phase 1: keep array-level helpers as pure numpy functions; **fix the
  `rebin_to_grid` shape inconsistency** (the one behavioural change); add
  `value_at`, `common_grid`, `align_to_grid`.
- Phase 2: `Spectrum` (`@dataclass`, single spectrum, full provenance, QF as
  a dict, xarray methods) and `SpectrumStack` (list-backed, lazy 2D gridded
  view, per-spectrum provenance arrays).
- Phase 3: source adapters in `io.py` for PANAGEA / Loisel23 / Tara.
- Phase 4: migrate callers and fix the stale `oceancolor` imports; drop dead
  `embed` imports.
- Phase 5: `test_spectra.py` (data-independent always-run + skip-guarded
  data tests, mirroring `test_panagea.py`).
- Phase 6: `nb/Spectra/Spectra_demo.ipynb` (matches `nb/PANAGEA/` style).
- Phase 7: `docs/spectra.rst` + API autodoc + changelog.
- Phase 8: verify with pytest in `ocean14` and run the demo notebook.

Checked repo conventions: notebooks live in `nb/<Topic>/`, tests in
`ocpy/tests/test_*.py` with a `$OS_COLOR` skip guard, Sphinx docs per topic
(`docs/panagea.rst` precedent). Raised five round-3 questions (Q14–Q18)
confirming the defaults the plan assumed (lazy gridding, ragged `to_xarray`
behaviour, netCDF scope, a `plot()` method, Loisel23 variable keys). No code
written yet — implementation awaits go-ahead.

### 2026-06-16 (Planning task 4: folded in round-3 answers, refined plan)

Read the user's answers to Q14–Q18 and updated **The Plan** accordingly:
- Q14 (lazy gridding): confirmed, no change.
- Q15: the user asked that the xarray representation carry **wavelength as a
  data Variable rather than a coordinate**. Reworked the xarray layout for
  both classes: a bare index dimension (`channel`) with `wavelength`,
  `values`, `errors` as data variables. A useful consequence — ragged stacks
  can now be serialized by NaN-padding to the max channel count, so
  `to_xarray()` no longer needs to raise on ragged input (the open half of
  Q15). Updated the "Decisions" note and the test phase (assert wavelength is
  a variable, add a ragged round-trip test).
- Q16: add `to_netcdf()`/`read_netcdf()` wrappers on both classes; added a
  tmp_path round-trip test.
- Q17: firmed up `plot()` (matplotlib, returns an Axes) + a smoke test.
- Q18: will confirm Loisel23 variable keys/units against the data during
  implementation.

Raised two minor round-4 confirmations (Q19 dim name `channel`; Q20 `date`
stored as ISO string in netCDF). Plan is otherwise ready to implement; still
no code written.

### 2026-06-16 (Planning task 5: final confirmations, plan ready)

Read the user's round-4 answers. Both confirmed the plan's existing defaults:
Q19 — the bare index dimension stays named `channel`; Q20 — `date` is
serialized to netCDF as an ISO-8601 string in `.attrs`. No structural changes
needed; folded both into the "Decisions" block and marked the plan **ready to
implement** (all questions answered). Planning is complete; implementation
awaits the go-ahead.

### 2026-06-16 (Implement Phase 0: module skeleton & relocation)

Executed Phase 0 of the Plan.

Done:
- Created `ocpy/spectra/utils.py` with the `rebin` and `rebin_to_grid`
  functions relocated verbatim from `ocpy/utils/spectra.py`, dropping only
  the dead `from IPython import embed` line. (No behaviour change here; the
  `rebin_to_grid` shape fix is Phase 1.)
- Deleted `ocpy/utils/spectra.py`.
- Verified in the `ocean14` env: `from ocpy.spectra import utils` imports and
  `utils.rebin` runs correctly; `import ocpy.spectra` succeeds.

Learned / decided:
- A repo-wide grep found **no** importers of `ocpy.utils.spectra`, so the
  relocation broke nothing. The four `from oceancolor.tara import spectra`
  hits target `ocpy/tara/spectra.py`, a separate module — those are Phase 4.
- Deferred populating `ocpy/spectra/__init__.py` (the planned `Spectrum` /
  `SpectrumStack` re-exports) and creating `core.py` / `io.py` until their
  phases, to avoid broken imports of not-yet-existing classes. Noted in the
  Implement Q&A.
- Git left to the user (per CLAUDE.md): the change is one new file
  (`ocpy/spectra/utils.py`) and one deletion (`ocpy/utils/spectra.py`).

### 2026-06-16 (Implement Phase 1: array-level helpers)

Built out `ocpy/spectra/utils.py` as pure numpy free functions (per the
"methods on basic objects" requirement), each with a "Generated by JXP and
Claude" docstring and <80-char lines.

Done:
- `rebin` — kept; added a docstring note that errors are interpolated as a
  co-located 1-sigma curve, not formally propagated (matches Q7).
- `rebin_to_grid` — **rewrote to the consistent spectrum-major
  `(nspec, nwave)` shape** (the planned behavioural change). 1D input is
  promoted to a 1-row stack. Also fixed empty-bin handling (values+errors →
  NaN; was a 0.0 latent bug) and suppressed all-NaN-bin divide warnings.
- New helpers: `value_at` (scalar-in/scalar-out interpolation),
  `common_grid` (tolerant equality test that drives `is_gridded`), and
  `align_to_grid` (rebin a ragged collection onto one grid → stacked 2D).
- Created `ocpy/tests/test_spectra.py` with 14 data-independent tests,
  including the `rebin_to_grid` shape regression test the Plan called for.
  All 14 pass in `ocean14`.

Learned / flagged:
- The `rebin_to_grid` shape flip means the Tara caller
  (`tara/explore.py`, which feeds `(nwave, nspec)` from `spectbl_from_keys`)
  will need a transpose at Phase 4. Recorded in the Implement Q&A.
- New file: `ocpy/spectra/utils.py` (rewritten) and
  `ocpy/tests/test_spectra.py`. Git left to the user.

### 2026-06-16 (Implement Phase 2: core classes)

Created `ocpy/spectra/core.py` with `Spectrum` and `SpectrumStack`
(both `@dataclass`), and wired up `ocpy/spectra/__init__.py`.

`Spectrum`: fields wavelength/values/errors + units/source/date/lat/lon/
depth/quality(dict)/metadata(dict). `__post_init__` coerces to float arrays,
validates lengths, and sorts ascending by wavelength. Methods (thin wrappers
over `utils`): `rebin` (interp/bin), `value_at`, `__len__`, `__repr__`,
`to_xarray`/`from_xarray`, `to_netcdf`/`read_netcdf`, and a matplotlib
`plot`. xarray layout puts **wavelength as a data variable** on a bare
`channel` dim; provenance goes to `.attrs` (None omitted, date as ISO string,
quality/metadata as JSON, `has_errors` flag so None errors round-trip).

`SpectrumStack`: list-backed, container protocol (`len`/`iter`/`getitem`),
`is_gridded` (via `utils.common_grid`), lazy `as_array()` (raises on ragged),
`rebin` → gridded stack, provenance accessors (`dates`/`lats`/`lons`/
`depths`/`sources`), and `to_xarray`/`from_xarray`/`to_netcdf`/`read_netcdf`.
The xarray form NaN-pads ragged members to the longest row (dims
`(spectrum, channel)`); the per-row wavelength variable makes the pad
unambiguous on read.

Tests: extended `test_spectra.py` to 32 tests (was 14). New coverage: Spectrum
construction/validation/sorting, rebin (interp+bin), value_at, xarray
round-trip (asserting wavelength is a variable not a coord), no-errors case,
netCDF round-trip via tmp_path, plot smoke test; SpectrumStack container
protocol, gridded/ragged detection, as_array raising, rebin→gridded,
provenance arrays, and gridded + ragged + netCDF round-trips. **All 32 pass
in `ocean14`.**

Decisions/deviations (logged in Implement Q&A): stack provenance stored as a
JSON attr rather than per-spectrum 1D variables (netCDF-safe, full fidelity;
trade-off is no in-xarray slicing of provenance). New files:
`ocpy/spectra/core.py`, updated `ocpy/spectra/__init__.py`,
`ocpy/tests/test_spectra.py`. Git left to the user.

### 2026-06-16 (Implement Phase 3: source adapters)

Created `ocpy/spectra/io.py` mapping the existing loaders into the classes
(heavy source modules imported lazily so `import ocpy.spectra.io` stays
cheap). Functions: `from_panagea` / `stack_from_panagea`,
`from_loisel23` / `stack_from_loisel23`, `from_tara` / `stack_from_tara`,
each with "Generated by JXP and Claude" docstrings.

Verified against real data in `ocean14`:
- **PANAGEA** (`load('rrs')`): `from_panagea` yields a sorted Spectrum,
  units `'1/sr'` from column metadata, source `'PANAGEA'`, provenance from
  the row; round-trips through xarray.
- **Loisel23** (`load_ds(1,0)`): `Lambda` is a shared coordinate (81 bands);
  variables `Rrs`/`a`/`aph`/`bb`/... carry **no units** in the file, so
  `from_loisel23` defaults `units=None` with a `units=` override. The stack
  is gridded; `as_array()` returns `(4, 81)`.
- **Tara**: no local data, so the adapter reuses `spectrum_from_row`, sets
  units `'1/m'`, and looks up lat/lon/date defensively; exercised on a
  synthetic row.

Tests: `test_spectra.py` now 36 (added 4 skip-guarded data-backed adapter
tests for PANAGEA + Loisel23). All 36 pass (data present, none skipped).

Deviations logged in Implement Q&A: `from_tara(row, ...)` instead of
`(tbl, row, ...)`; Loisel23 units left None by default (files carry none).
New file: `ocpy/spectra/io.py`; updated `ocpy/tests/test_spectra.py`. Git
left to the user.